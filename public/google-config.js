// Paste a Google OAuth Web client ID (public). Setup steps are in the README.
window.GOOGLE_CONFIG = {
  clientId: "556658401647-soqsutprr74h5723vdcm03sfaikus68a.apps.googleusercontent.com",
  // Used only when joining an existing household (Google Picker). Create an API key in the
  // same Cloud project and enable the "Google Picker API". See the README.
  apiKey: "",
  // Cloud project number for the picker; derived from clientId when left blank.
  appId: ""
};
